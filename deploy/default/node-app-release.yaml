---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: node-app
  namespace: default
spec:
  chart:
    spec:
      chart: node-app
      version: 0.3.0
      sourceRef:
        kind: HelmRepository
        name: mycharts
        namespace: flux-system
  interval: 1m0s

